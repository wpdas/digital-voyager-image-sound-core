/**
 * WAV Header bytes size
 */
export const WAV_HEADER_BYTES_SIZE = 44;

/**
 * Each tone registers 176 bytes of information.
 */
export const WAV_TONE_BYTES_SIZE = 176;

/**
 * Tone frequency
 */
export const WAV_TONE_FREQUENCY = 1000;

/**
 * Typeid uses 8 bits of information
 */
export const TYPE_ID_BITS_SIZE = 8;
